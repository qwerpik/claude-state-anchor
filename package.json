{
  "name": "claudestateanchor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin: protects session state across context compaction.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
